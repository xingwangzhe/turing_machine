import { PixiApp } from './application.js';
import { Events } from './events.js';
import { GlobalStyle } from './global-style.js';
import { LoadOhMyLive2D } from './load-oml2d.js';
import { Menus } from './menus.js';
import { Models } from './models.js';
import { Stage } from './stage.js';
import { StatusBar } from './status-bar.js';
import { Store } from './store.js';
import { Tips } from './tips.js';
import type { DefaultOptions, PixiLive2dDisplayModule, PixiModule } from '../types/index.js';
export declare class OhMyLive2D {
    private globalOml2d;
    private events;
    private PIXI;
    private PixiLive2dDisplay;
    store: Store;
    globalStyle: GlobalStyle;
    stage: Stage;
    statusBar: StatusBar;
    tips: Tips;
    menus: Menus;
    models: Models;
    pixiApp?: PixiApp;
    currentModelIndex: number;
    options: DefaultOptions;
    constructor(globalOml2d: LoadOhMyLive2D, events: Events, PIXI: PixiModule, PixiLive2dDisplay: PixiLive2dDisplayModule);
    /**
     * 移动端是否隐藏
     */
    private get mobileHidden();
    private set modelIndex(value);
    get modelIndex(): number;
    /**
     * 创建
     */
    private create;
    /**
     * 挂载
     */
    private mount;
    /**
     * 加载模型
     */
    private loadModel;
    /**
     * 重新加载
     */
    reloadModel(): Promise<void>;
    /**
     * 加载下个模型
     */
    loadNextModel(): Promise<void>;
    initialize(): void;
    /**
     * 注册全局事件
     */
    private registerGlobalEvent;
}
